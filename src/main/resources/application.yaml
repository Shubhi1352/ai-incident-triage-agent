spring:
  application:
    name: ai-incident-triage-agent

  datasource:
    url: jdbc:postgresql://localhost:5432/ai_triage_db
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080